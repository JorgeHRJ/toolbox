{% extends 'layout.html.twig' %}

{% page_setup {
    'title': 'Balsas',
    'header': 'Balsas',
    'subheader': 'Estadísticas de las balsas de La Palma',
    'breadcrumbs': [
        {
            'name': 'Balsas',
            'main': true
        }
    ],
    'body': {
        'attr': {
            'controller': 'reservoir',
            'page': 'index'
        }
    }
} %}

{% block content %}
    <div class="app-card shadow-sm mb-2">
        <div class="inner">
            <div class="app-card-body p-3 p-lg-4">
                <div class="row gx-5 gy-3 justify-content-center">
                    <canvas width="500" height="500" data-component="reservoir-detail-stats"></canvas>
                </div>
            </div>
        </div>
    </div>
    <div class="row g-4">
        {% for item in data %}
            <div class="col-6 col-md-4 col-xl-3 col-xxl-2" data-component="reservoir-item">
                <div class="app-card app-card-stat shadow-sm h-100">
                    <div class="app-card-body p-3 p-lg-4">
                        <h4 class="stats-type mb-1" data-name="{{ item.reservoir.name }}">
                            {{ item.reservoir.name }}
                        </h4>
                        <div class="stats-figure">
                            <span data-fillness="{{ item.fillness }}">{{ item.fillness }} %</span>
                        </div>
                        <div class="stats-meta text-success">
                            <ul class="list-unstyled mb-0">
                                <li>
                                    <span class="text-muted">Municipio:</span>
                                    {{ item.reservoir.municipality.name }}
                                </li>
                                <li>
                                    <span class="text-muted">Capacidad:</span>
                                    {{ item.reservoir.capacity }} m<sup>3</sup>
                                </li>
                                <li>
                                    <span class="text-muted">Fecha actualización:</span>
                                    {{ item.process.date|date('d/m/Y') }}
                                </li>
                            </ul>
                        </div>
                    <a class="app-card-link-mask"
                       href="{{ path('reservoir_detail', {'id': item.reservoir.id}) }}"></a>
                </div>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}
